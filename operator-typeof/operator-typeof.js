// ---------------------------------------------------------------------------------------------------- //

        // PENERAPAN OPERATOR TYPEOF
        document.writeln("PENERAPAN OPERATOR TYPEOF PADA JAVA SCRIPT");
        document.writeln("</br>");

        // membuat variable
        let data;
        const typeData = typeof data; // membuat variable untuk mengeksekusi typeof
        
        // cetak
        document.writeln(`<p>Tipe data dari 'data' adalah ${typeData} </p>`);

// --------------------------------------------------------------------------- //

        // contoh lain
        let data1 = null;
        const typeData1 = typeof data1; // membuat variable untuk mengeksekusi typeof
        
        // cetak
        document.writeln(`<p>Tipe data dari 'data1' adalah ${typeData1} </p>`);

// --------------------------------------------------------------------------- //

        // contoh lain
        let data2 = true;
        const typeData2 = typeof data2; // membuat variable untuk mengeksekusi typeof
        
        // cetak
        document.writeln(`<p>Tipe data dari 'data2' adalah ${typeData2} </p>`);

// --------------------------------------------------------------------------- //

        // contoh lain
        let data3 = "Wafi";
        const typeData3 = typeof data3; // membuat variable untuk mengeksekusi typeof
        
        // cetak
        document.writeln(`<p>Tipe data dari 'data3' adalah ${typeData3} </p>`);

// --------------------------------------------------------------------------- //

        // contoh lain
        let data4 = 700;
        const typeData4 = typeof data4; // membuat variable untuk mengeksekusi typeof
        
        // cetak
        document.writeln(`<p>Tipe data dari 'data4' adalah ${typeData4} </p>`);
